[package]
name = "nanna-simd"
version.workspace = true
edition.workspace = true

[dependencies]
half.workspace = true

# `wide` backs the AVX2 path only; on other targets it would be unused.
[target.'cfg(target_arch = "x86_64")'.dependencies]
wide.workspace = true

[features]
default = []
nightly = []  # Enable std::simd when on nightly
