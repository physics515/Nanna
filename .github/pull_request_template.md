## What and why

<!-- What does this change, and what problem does it solve? Link the issue: Fixes #123 -->

## How it was tested

<!-- Tests added or run (`cargo test -p <crate>`, GUI checks), and anything verified by hand. -->

## Checklist

- [ ] `cargo clippy --workspace --all-targets` is clean, with no new `#[allow]` / `#[expect]`
- [ ] Tests cover the change
- [ ] README.md updated if user-facing behavior changed
