# Rust
target/
target-check/
# Cargo.lock is committed for reproducible builds + reviewable dependency bumps
# (this is a workspace with binaries, so pinning the lockfile is the right call; see ROADMAP P13 / daily-dev Appendix B).

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Config with secrets
config.toml

# Local data
*.db
*.db-shm
*.db-wal

rustc-ice-*.txt

nul
